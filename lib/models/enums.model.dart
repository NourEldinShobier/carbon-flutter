enum CWidgetSize {
  large,
  medium,
  small,
}

enum CWidgetState {
  enabled,
  hover,
  active,
  focus,
  disabled,
}

enum CWidgetStatus {
  primary,
  success,
  info,
  warning,
  danger,
}

enum CPositioning {
  left,
  right,
  none,
}

enum CVerticalPositioning {
  top,
  bottom,
}
